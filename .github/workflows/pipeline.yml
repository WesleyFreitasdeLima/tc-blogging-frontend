name: CI/CD Pipeline

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Compile TypeScript and Build Vite
        run: npm run build

  dockerhub:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile.prod
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/tc-blogging-frontend:latest
          # Injects the repository variable into the Docker build context for Vite compilation
          build-args: |
            VITE_API_URL=${{ vars.BACKEND_API_URL }}
          # Forces a clean build by bypassing layer cache to ensure environment variables are evaluated
          no-cache: true

  render:
    needs: dockerhub
    runs-on: ubuntu-latest
    steps:
      - name: Run Deploy Hook
        # Triggers Render's webhook to pull the latest image and update production
        run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
